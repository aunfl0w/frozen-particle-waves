#!/bin/bash 

cd "$(dirname ${BASH_SOURCE[0]})"

. ./settings


wget -q -O - http://localhost:8081/metrics |jq
wget -q -S -X POST -O - http://localhost:8081/shutdown
sleep 5
ps aux  |grep "[f]rozen-particle" |while read uid processid rest
do
echo stopping $rest
kill -9 $processid
done

